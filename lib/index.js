var async = require('async');

// settings
module.exports = function batchEndpoint(server, options) {
    if (!options)
        options = {};
};

// restify requests
module.exports = function (req,res,next)
{
  next();
};
